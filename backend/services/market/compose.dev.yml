services:
  market-db:
    environment:
      POSTGRES_HOST_AUTH_METHOD: trust

  market:
    build:
      target: development
    ports: ["8000:8000"]
    command: uvicorn app.main:app --host 0.0.0.0 --port 8000
    volumes:
      - ./app:/app/app
      - ../../shared:/shared
