# nginx/nginx.conf
events {}

http {
    upstream frontend {
        server frontend:3000;
    }

    upstream backend {
        server backend:8000;
    }

    # upstream supertokens {
    #     server supertokens:3567;
    # }

    server {
        listen 80;
        server_name localhost;

        # Frontend
        location / {
            proxy_pass http://frontend;
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            proxy_set_header X-Forwarded-Proto $scheme;
        }

        location /api/ {
            proxy_pass http://backend;
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            proxy_set_header X-Forwarded-Proto $scheme;
        }

        # SuperTokens API - важно правильно настроить заголовки
        # location /auth/ {
        #     proxy_pass http://supertokens/;
        #     proxy_set_header Host $host;
        #     proxy_set_header X-Real-IP $remote_addr;
        #     proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        #     proxy_set_header X-Forwarded-Proto $scheme;
        #     
        #     # Критически важные заголовки для SuperTokens
        #     proxy_pass_request_headers on;
        #     add_header Access-Control-Allow-Origin "$http_origin" always;
        #     add_header Access-Control-Allow-Credentials "true" always;
        #     add_header Access-Control-Allow-Methods "GET, POST, OPTIONS, PUT, DELETE" always;
        #     add_header Access-Control-Allow-Headers "Content-Type, Authorization, st-auth-mode, rid, fdi-version, front-token" always;
        #     add_header Access-Control-Expose-Headers "front-token" always;
        #     
        #     # Обработка preflight запросов
        #     if ($request_method = OPTIONS) {
        #         return 204;
        #     }
        # }
    }
}
